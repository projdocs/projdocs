{
  "name": "@packages/go",
  "scripts": {
    "gen:types": "supabase gen types --local --lang=go > database/types.go",
    "gen:migrations": "rm -rf ./migrations/migrations && cp -r ../../supabase/migrations ./migrations/migrations",
    "gen": "npm run gen:types && npm run gen:migrations && npm run gen:migrations:post",
    "gen:migrations:post": "node -e \"const fs = require('fs');const path = require('path');const dir = './migrations/migrations' || '.';fs.readdirSync(dir).forEach(file => {const newName = file.split('_')[0] + path.extname(file);if (newName !== file)fs.renameSync(path.join(dir, file), path.join(dir, newName));});\""
  }
}